Config = {}
Config.ServerIP = 'http://img.goldfish.sbs:8080' -- Public IP + Port to your Python Server
Config.Token = 'YlfQbkSoE01sCYb1BdVRGjytJ9cYNnmj'
Config.UseDiscordWebhook = false -- [webhook/false]
-- If the above is set to false, normal python server will be used for uploads
-- If you do not have the python server and you do not want to create it, set the webhook and the images will be uploaded to the webhook
-- The webhook is VISIBLE for clients and they get it.