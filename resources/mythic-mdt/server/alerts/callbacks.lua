function RegisterEACallbacks()
	-- Callbacks:RegisterServerCallback("EmergencyAlerts:GetCallsign", function(source, data, cb)
	-- 	local ply = Fetch:CharacterData("ID", data)
	-- 	if ply ~= nil then
	-- 		cb(ply:GetData("Character"):GetData("Callsign"))
	-- 	else
	-- 		cb(nil)
	-- 	end
	-- end)
end